project:
  name: "ArXiv-Multi-Agent-Assistant"
  version: "1.0.0"

data:
  output_dir: "./data"           # 所有資料的根目錄
  pdf_dir: "pdfs"                # PDF 存放子目錄
  metadata_file: "metadata.json" # 抓取的原始論文資訊

system:
  logging_level: "INFO"
  device: "cpu"                  # 若有 NVIDIA 顯卡可改為 "cuda" (針對 Vector Agent)

# === 1. 抓取代理設定 ===
scraper:
  keywords: 
    - "Multi-Agent Systems"
    - "Large Language Model Agents"
    - "RAG Systems"
  max_results: 30                # 每次執行抓取的最大篇數 (建議測試時設小一點)
  sort_by: "submittedDate"       # 排序方式
  retry_attempts: 3              # 下載失敗重試次數
  sleep_interval: 2              # 請求間隔秒數 (禮貌爬蟲)

# === 2. 解析代理設定 ===
parser:
  chunk_size: 1000               # 每個文字塊的字元數 (約 250-400 tokens)
  chunk_overlap: 100             # 文字塊間的重疊部分
  ignore_references: true        # 是否自動去除 "References" 之後的內容
  output_file: "parsed_papers.json" # 解析後的結構化資料

# === 3. 向量代理設定 ===
vector:
  model_name: "all-MiniLM-L6-v2"      # HuggingFace Embedding 模型
  index_file: "faiss_index.bin"       # FAISS 索引存檔
  chunks_map_file: "chunks_map.json"  # ID 與文字對照表
  batch_size: 32                      # 向量轉換的批次大小

# === 4. 摘要代理設定 (Ollama) ===
summarizer:
  model_name: "llama3.2"
  
  # 修改 System Prompt：強制要求英文回答
  system_prompt: |
    You are an expert academic research assistant. 
    Your goal is to summarize technical papers clearly and concisely for a developer and researcher audience.
    Always answer in English. Use professional academic tone.

  # 修改 Prompt Templates：將問題改為英文
  prompts:
    quick_summary: |
      Here is the abstract and key sections of a paper:
      "{text}"
      
      Please provide a structured summary:
      1. **Core Problem**: What specific problem is this paper trying to solve?
      2. **Methodology**: What approach or architecture did they propose?
      3. **Key Results**: What were the main findings or improvements achieved?
      
      Keep it concise and use bullet points where appropriate.

    detailed_report: |
      Context from the paper:
      "{text}"
      
      Please generate a comprehensive report with the following sections:
      ## 1. Background & Motivation
      ## 2. Core Methodology & Innovation
      ## 3. Experimental Results
      ## 4. Conclusion & Future Work

# === 5. 報告代理設定 (UI) ===
reporter:
  graph_output_file: "knowledge_graph.html"
  app_title: "🤖 ArXiv Multi-Agent Assistant"
  theme_color: "#FF4B4B"
